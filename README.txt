This is a single multi-user chat server done through sockets and threading.

The server takes in users that connect to the IP address and port, receiving and propagating messages to all connected users

Users disconnect by entering a keyword

Server holds all the chat logs but does not store any messages

